node {
  stage('Checkout') {
    checkout scm
  }
  stage('Build') {
    echo 'Bla bla bla'
  }
  stage('Test') {
    echo 'I run some test'
  }
  stage('Upload to repository') {
    echo 'upload done'
  }
}
